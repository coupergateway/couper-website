import React from 'react'

import CookieLayer from '.'

export default {
  title: 'Components/CookieLayer',
  component: CookieLayer,
}

const Template = () => (
  <CookieLayer />
)

export const Full = Template.bind({})
