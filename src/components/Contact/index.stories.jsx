import React from 'react'

import Contact from '.'

export default {
  title: 'Components/Contact',
  component: Contact,
}

const Template = () => (
  <Contact />
)

export const Full = Template.bind({})
