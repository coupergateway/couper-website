import React from 'react'

import Brands from '.'

export default {
  title: 'Components/Brands',
  component: Brands,
}

const Template = () => (
  <Brands />
)

export const Full = Template.bind({})
