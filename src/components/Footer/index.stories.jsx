import React from 'react'

import Footer from '.'

export default {
  title: 'Components/Footer',
  component: Footer,
}

const Template = () => (
  <Footer />
)

export const Full = Template.bind({})
